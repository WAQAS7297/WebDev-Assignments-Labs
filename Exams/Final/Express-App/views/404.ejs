<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= pageTitle %></title>
    <link href="/stylesheets/style.css" rel="stylesheet"> <!-- Link your main CSS -->
</head>
<body>
    <div class="container">
        <h1>404 - <%= pageTitle %></h1>
        <% if (typeof message !== 'undefined' && message) { %>
            <p><%= message %></p>
        <% } else { %>
            <p>Sorry, the page you are looking for cannot be found.</p>
        <% } %>
        <a href="/">Go to Homepage</a>
    </div>
</body>
</html>