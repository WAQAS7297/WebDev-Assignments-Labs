<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Submit Complaint - Tehzeeb Bakers</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .complaint-container {
            max-width: 600px;
            margin: 40px auto;
            padding: 20px;
            padding-top:170px;
        }
        .form-control:focus {
            border-color: #A4854F;
            box-shadow: 0 0 0 0.2rem rgba(164, 133, 79, 0.25);
        }
    </style>
</head>
<body>
    <div class="complaint-container">
        <h1 class="text-center mb-4">Submit Complaint</h1>
        
        <% if (locals.success) { %>
            <div class="alert alert-success"><%= locals.message %></div>
        <% } else if (locals.error) { %>
            <div class="alert alert-danger"><%= locals.error %></div>
        <% } %>

        <div class="complaint-form">
            <form action="/submit-complaint" method="POST">
                <div class="mb-3">
                    <label for="orderId" class="form-label">Order ID</label>
                    <input type="text" class="form-control" id="orderId" name="orderId" required>
                </div>
                <div class="mb-3">
                    <label for="message" class="form-label">Complaint Message</label>
                    <textarea class="form-control" id="message" name="message" rows="5" required></textarea>
                </div>
                <div class="text-center">
                    <button type="submit" class="btn btn-primary px-4">Submit Complaint</button>
                </div>
            </form>
        </div>

        <div class="text-center mt-4">
            <a href="/mycomplaints" class="btn btn-secondary">View My Complaints</a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>