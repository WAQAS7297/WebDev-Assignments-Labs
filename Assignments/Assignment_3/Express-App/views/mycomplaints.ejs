<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Complaints - Tehzeeb Bakers</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .complaints-container {
            max-width: 1200px;
            margin: 40px auto;
            padding: 20px;
            padding-top:170px;
        }
        .status-badge {
            padding: 5px 10px;
            border-radius: 15px;
        }
        .pending { background-color: #ffc107; color: #000; }
        .resolved { background-color: #28a745; color: #fff; }
        .in_progress { background-color: #17a2b8; color: #fff; }
    </style>
</head>
<body>
    <div class="complaints-container">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="mb-0">My Complaints</h1>
            <a href="/complaint" class="btn btn-primary">Submit New Complaint</a>
        </div>
        
        <% if (complaints.length === 0) { %>
            <div class="alert alert-info">You have no complaints submitted.</div>
        <% } else { %>
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>Order ID</th>
                            <th>Message</th>
                            <th>Status</th>
                            <th>Submitted</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% complaints.forEach(complaint => { %>
                            <tr>
                                <td><%= complaint.orderId %></td>
                                <td><%= complaint.message %></td>
                                <td>
                                    <span class="status-badge <%= complaint.status %>">
                                        <%= complaint.status %>
                                    </span>
                                </td>
                                <td><%= new Date(complaint.createdAt).toLocaleString() %></td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>